# Example environment file for local development
# Copy to `.env` and fill with real values (do NOT commit `.env`)

# Preferred: set full DATABASE_URL (12-factor)
# DATABASE_URL=postgresql+psycopg2://postgres:supersecret@localhost:5432/sistema_escolar

# Or provide granular settings (used only if DATABASE_URL is not set):
DB_HOST=localhost
DB_NAME=sistema_escolar
DB_USER=postgres
DB_PASSWORD=supersecret
DB_PORT=5432

# Flask debug control
# FLASK_DEBUG=1
